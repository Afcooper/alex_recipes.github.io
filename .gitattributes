# Normalize line endings to LF everywhere. The build writes recipes.js with LF;
# forcing LF in the working tree too avoids spurious diffs (and CI rebuild loops)
# between Windows commits and the Linux GitHub Action.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
